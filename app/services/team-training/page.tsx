import PageClient from "./page-client";

export const metadata = {
  title: "Team Training & Certification — SynapseCore",
  description: "Enterprise-ready training and certification to build security and AI capability across your organization.",
};

export default function TeamTrainingPage() {
  return <PageClient />;
}

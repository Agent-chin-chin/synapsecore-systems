import PageClient from "./page-client";

export const metadata = {
  title: "Website Development — SynapseCore",
  description: "Enterprise web development: secure, scalable, and integration-ready applications.",
};

export default function WebDevPage() {
  return <PageClient />;
}

import PageClient from "./page-client";

export const metadata = {
  title: "Personalized Dashboard | SynapseCore",
  description: "Customizable security dashboard with widgets, analytics, and user-specific insights.",
};

export default function PersonalizedDashboardPage() {
  return <PageClient />;
}

import ResourceCenterPageClient from './page-client';

export const metadata = {
  title: "Resource Center | SynapseCore",
  description: "Whitepapers, eBooks, webinars, tutorials, and blog articles for cybersecurity and development.",
};

export default function ResourceCenterPage() {
  return <ResourceCenterPageClient />;
}

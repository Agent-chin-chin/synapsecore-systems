import NewsletterPageClient from './page-client';

export const metadata = {
  title: "Newsletter | SynapseCore",
  description: "Subscribe to the SynapseCore newsletter for cybersecurity news, product updates, and insights.",
};

export default function NewsletterPage() {
  return <NewsletterPageClient />;
}

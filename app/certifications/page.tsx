import PageClient from "./page-client";

export const metadata = {
  title: "Certifications | SynapseCore",
  description: "SynapseCore certifications, compliance frameworks, and security accreditations.",
};

export default function CertificationsPage() {
  return <PageClient />;
}
